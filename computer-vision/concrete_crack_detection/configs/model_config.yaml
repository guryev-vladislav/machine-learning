# configs/model_config.yaml
unet:
  in_channels: 3
  out_channels: 1
  features: [64, 128, 256, 512]

classifier:
  num_classes: 2
  pretrained: true
  backbone: "efficientnet_b0"

training:
  batch_size: 16
  learning_rate: 0.001
  weight_decay: 0.0001
  epochs: 100